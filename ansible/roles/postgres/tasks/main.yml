---
- name: Install PostgreSQL
  ansible.builtin.apt:
    name:
      - postgresql
      - postgresql-contrib
    state: present

- name: Enable PostgreSQL
  ansible.builtin.systemd:
    name: postgresql
    enabled: true
    state: started
